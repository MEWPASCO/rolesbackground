:root {
  --opacity-role-design: 50%;
}

/* Target the role elements and apply styles */
.role_dfa8b6{
  transform: translate(0);
  border: 0;
  border-radius: 5px !important;
  --transparency: 0.4; /* You may want to adjust the transparency value */
}

/* Target the roleRemoveIcon and adjust its positioning */
.role_dfa8b6 .roleRemoveIcon_dfa8b6 {
  left: 12px;
  transform: translate(-50%, -50%);
  margin: 0;
}

/* Reset the positioning of roleRemoveButton */
.role_dfa8b6 .roleRemoveButton_dfa8b6 {
  position: static;
}

/* Target the roleCircle element and apply additional styles */
.role_dfa8b6 .roleCircle__4f569:before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  border-radius: 5px;
  box-sizing: border-box;
  background: inherit;
  opacity: var(--opacity-role-design) !important;
  z-index: -1 !important;
}

/* Fix the text being too blurry */
.roleNameOverflow_27401 {
  color: hsl(0, 0%, 75%);
}
